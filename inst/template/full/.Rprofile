# insert this before the package installations in Dockerfile
# and .github/workflows/docker.yaml if you want to block
# package version using release date:
# options(repos = list(CRAN = "https://cran.microsoft.com/snapshot/2020-03-26/"))
source("renv/activate.R")

